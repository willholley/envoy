language: node_js

node_js:
  - "5"

git:
  depth: 30

sudo:
  false

services:
  - docker

env:
  global: 
  - PORT=8080
  - MBAAS_DATABASE_NAME='mbaas'
  matrix:
  - COUCH_HOST=http://127.0.0.1:3001

script:
  npm run test
